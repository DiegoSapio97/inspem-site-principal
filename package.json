{
  "name": "inspem-site-principal",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4329",
    "build": "astro build",
    "test": "npm run build && node --test tests/*.test.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@fontsource/newsreader": "^5.3.0",
    "astro": "^7.2.4"
  }
}
